/* import { Hizb } from "@/types";

export const isLastAyainHizb = ({
  aya,
  surah,
}: {
  aya: number;
  surah: number;
}) => {
  const hizb: Hizb[] = hizbJson as Hizb[];
  const lastAyainHizb = hizb[index].last_verse_key.split(':');
  return (
    lastAyainHizb[0] === surah.toString() && lastAyainHizb[1] === aya.toString()
  );
}; */
